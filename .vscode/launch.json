{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "attach",
      "name": "Docker: Attach to Node in todolist",
      "remoteRoot": "/todolist",
      "localRoot": "${workspaceFolder}"
    }
  ]
}
